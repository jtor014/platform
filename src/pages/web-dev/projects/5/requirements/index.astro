---
import DocsLayout from '../../../../../layouts/DocsLayout.astro';
import Card from '../../../../../components/Card.astro';
---

<DocsLayout title="Requirements" description="Meet the client, understand the brief, and write the PRD with a permissions matrix.">
  <h1 class="mb-6">Requirements</h1>
  <p class="text-text-secondary mb-4">You know the process. Client brief, prompt plus template, review. The new wrinkle: this PRD needs to specify permissions for every feature. Who can see what, who can do what, and what happens when someone tries to access something they shouldn't.</p>

  <p class="text-text-secondary mb-8">Work through these in order:</p>

  <div class="grid gap-4">
    <Card title="1. The client brief" subtitle="Meet Priya and understand what DeskHub needs" href="/web-dev/projects/5/requirements/client-brief" />
    <Card title="2. Writing the PRD" subtitle="Create the requirements document with a permissions matrix" href="/web-dev/projects/5/requirements/writing-the-prd" />
  </div>
</DocsLayout>
